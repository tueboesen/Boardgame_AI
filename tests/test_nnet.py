import pytest

def test_nnetwrapper(game,neuralnet,args):
    """
    This should test the nnet wrapper class, and ensure:

    the output from the network has the correct dimensions
    the input examples have the right dimensions
    assert that the train routine actually trains the neural network

    assert that it works with cuda and without (only test cuda if available)
    """
    pass
